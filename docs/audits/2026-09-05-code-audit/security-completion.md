# Security/background completion review — T29–T38

This is a requirement-specific review of the current local change set, not a production deployment certification. No live mutations were performed. Detailed commands and earlier red reproductions are in [security-export-ai-validation.md](security-export-ai-validation.md).

| Requirement | Implementation and executable evidence | Remaining gap / rollout condition |
| --- | --- | --- |
| T29 — Same-owner DB/API references | Migration054 protects Detection→Buck, Match→Buck, Photo→batch/camera/location, batch→session, and Buck reference pointers. Confirm/correct APIs reject foreign Buck IDs and failed writes. Actual PGlite reproduces the original source-only RLS hole, then rejects foreign references and permits valid own references; matching regression tests cover unauthorized correction and ignored write failure. | Local source and disposable PostgreSQL verified. Requires migration054 plus matching application rollout. Existing historically poisoned rows are not automatically deleted; hardened read joins exclude foreign relationships. |
| T30 — Privileged storage signing | Migration054 validates original owner prefix and deterministic photo variant paths, makes crop pointers worker-managed, and derives public Showcase medium paths from owned images. Matching validates flat crop path shape; legacy crop UUIDs intentionally need not equal detection UUIDs. Single/batch server URL cache now requires authenticated user partition. PGlite path traversal/foreign variants/immutable crop/Showcase assertions and two signed-cache regressions pass. | Legacy worker-generated crop pointers remain supported; shape validation alone does not retrospectively prove provenance of every historical pointer. Sampled deployed table grants/RLS were read successfully (T32); storage policies and historical pointer provenance were not included in that metadata query. |
| T31 — Account state/requests | Identity persists per QueryClient; first auth clears unknown cache without reload, real account changes clear stores, synchronously hide private subtree, and navigate the document. Same-user refresh preserves state. Uploads abort/remove unload guard; late session callbacks cannot restore old state. Six real QueryClient/QueryObserver tests and active-run EventTarget regression pass; URL cache account tests pass. | Browser account navigation evidence belongs to root integration report. This does not revoke already-issued signed URLs in other devices or change their documented expiry. |
| T32 — Live grants and rollout | Correct-target live metadata read succeeded after adding the CA published by the official Supabase dashboard source, retaining `verify-full` TLS. Latest deployed migration is053; all six sampled tables have RLS and authenticated UPDATE grants. Their24policies scope mutations to source-owner rows. Six sampled definer RPC overloads allow authenticated execution and deny anon; claim_photo_work is absent. Artifact `.gstack/triage-runs/simulator/live-readonly-metadata.json`. Wrong-project connector was not used. | **Sampled live grants verified read-only; fixes not deployed.** Apply/review054–061 and worker rollout before claiming live remediation. Query did not inventory all schema triggers, storage policies, column-grant combinations or historical inconsistent data. |
| T33 — Worker exports/backpressure | Worker helpers accept an explicit admin client; no request-cookie dependency. ZIP consumption starts immediately into a byte-limited temporary file; appends await entry completion, final ZIP streams to storage, cleanup runs in finally. Real26MiB archiver regression passes; unknown-size overflow rejects. Small2–25 route now uses the same bounded spool and cleans on body completion/cancel. | Requires app and Trigger export-worker deployment. Runtime temporary disk, worker duration, storage upload and signed download must still be smoke-tested on the deployed target. |
| T34 — Export boundaries/results | Actual route tests cover25 streamed ZIP with25 central-directory entries,26/500 background jobs,501 rejection, and missing-original failure. Before fix the25-photo missing-original case returned200; now500 with retry message. Worker partial missing-original exports now fail explicitly rather than silently produce an incomplete ZIP. Polling tests validate nested/top-level progress and failed output. Known oversized selections return413 with split guidance; actual ZIP guard handles unknown metadata under500MiB. | This preserves500-photo selection cap but cannot fit arbitrarily large originals into the existing500MiB export bucket. User must split oversized selections. No hosted download/upload was performed. |
| T35 — Candidate merge/split | Merge uses HEAD count metadata; partial split keeps remaining candidate pending and chooses a remaining representative. Actual service fixtures assert exact merged count and remaining visibility. Numeric live-member filtering was subsequently added by gallery work. | No database transaction was added across a whole merge/split; concurrent conflicting operators can still require a future atomic RPC. Normal single-operation correctness is tested; no live concurrent mutation stress was performed. |
| T36 — Numeric Score readers | Migration061 derives is_trophy from authoritative score_gross versus owner threshold and refreshes on threshold changes. Active fingerprint/trophy readers and both guarded stats overloads use that flag. Actual PGlite proves impression-trophy/gross100 excluded, impression-standard/gross180 included, forged flags overwritten, threshold changes applied, and foreign stats denied. Gallery regression tests cover reader/candidate eligibility. | Requires061 after059 plus new readers deployed together. Qualitative size_class remains intentionally visible as a separate impression, not Trophy eligibility. |
| T37 — Gemini bounded model/retry/cost exposure | Explicit primary and at most one fallback; retired1.5/2.0 rejected. Structured429/503, HTTP Retry-After, SDK RetryInfo, jitter, per-attempt abort deadline and total retry budget tested. Default3 attempts per model, configurable max5; an enabled fallback permits two model budgets (up to6 attempts by default,10 maximum). Default shared5 jobs×2 crop calls. Synthetic1000-job fixture caps at3000 attempts; actual client mock verifies signal and explicit fallback wiring. | These are concurrency/retry exposure bounds, **not a dollar budget or verified project RPM/TPM allowance**. Check actual project/model/tier in AI Studio before rollout. Cancellation may not prevent charges for already accepted requests. No billable calls or vendor throughput tests performed. |
| T38 — Trigger limits/idempotency/terminal failure | Client25-file batches, API100 cap, shared five transfers; completion key includes batch/ready-ID digest with30-day TTL and per-photo analysis/variant keys. Lease claims and terminal counter idempotency tested in PGlite; actual completion-route lost-response503 test preserves key. Upload suite exercises1000 bounded transfers and handoff failure. Shared paid queue is declared by all paid task types. | Recovery coordinator must be deployed/scheduled independently of Vercel. Upload agent final evidence:36tests pass; invalid batch rate is clamped1–100. PGlite validates three persisted claims/no fourth, expired budgets become terminal exactly once, foreign/direct budget reset denied, and explicit owner Retry resets only failed work while preserving completed output (`/tmp/tinesight-db-budget-validation.log`). No actual hosted scheduler or Trigger vendor payload/throughput test was performed. |

## Rollout sequence and verification

1. Correct app project and trusted TLS are now confirmed; latest live ledger entry053 and sampled grants/RLS recorded. Recheck immediately before rollout and include any additional storage/trigger grants required by deployment review. The wrong-project connector cannot substitute for this check.
2. Review/apply the repository migrations in order through061, including054 ownership/storage guards,058 readiness/lifecycle/claims/recovery fields,059 triage/review,060 original hashes, and061 numeric Trophy synchronization. Review any existing ledger drift before applying; this report does not authorize blind replay.
3. Deploy compatible web/API and Trigger jobs, including the scheduled recovery coordinator and shared paid-task queue settings. Database-only or Vercel-only rollout is incomplete. Existing long-lived workers must not retain the old counter/claim behavior.
4. Run a small two-account smoke test on the intended environment: own/foreign relationship denial, original readiness, retry/cancellation, variant completion, numeric threshold refresh, export/download, and account swap. Read-only source tests and the root HTTP emulator cannot certify hosted grants, worker schedules, bucket limits, provider quotas, or real mobile memory.

## Latest focused evidence

- `lib/export/export-regression.test.ts`: real archiver, worker service context,500-ID chunking, status DTOs,25/26/500/501 routing, missing-original failures. Red route output: `/tmp/tinesight-export-boundary-red.log`; final output: `/tmp/tinesight-export-boundary-green.log`.
- `lib/auth/account-boundary.test.ts`, `lib/cache/signed-url-cache.test.ts`, `lib/upload/active-run.test.ts`: cache/observer/signal/account partition regressions.
- `scripts/verify-security-invariants.mjs` with `TINESIGHT_TEST_UPLOAD=1`: actual disposable PostgreSQL054/058/059/060/061 checks; no production connection.
- `lib/gemini/retry.test.ts`, `capacity.test.ts`: deterministic faults, model fallback, budgets; no provider calls.

Final focused combined security/export/auth/Gemini/view-refresh suite: **35 passed**, output `/tmp/tinesight-security-final-tests.log`. Full TypeScript check passed immediately after the export fixes; the final root combined gate passed162 tests, type-check, production build and changed-code lint (completion-review.md). Small-export route scoped ESLint: zero findings.

## Trusted TLS follow-up

`node --use-system-ca` and `node --use-openssl-ca` both retained `SELF_SIGNED_CERT_IN_CHAIN`. Supabase documents downloading its database CA for hostname-verified connections ([official SSL guide](https://supabase.com/docs/guides/platform/ssl-enforcement)). The official dashboard source identifies the production certificate endpoint ([custom-content.json](https://github.com/supabase/supabase/blob/master/apps/studio/hooks/custom-content/custom-content.json)); its downloaded certificate has SHA256 fingerprint `80:70:25:AD:50:D4:ED:21:9D:2C:9C:7D:29:9C:00:4F:82:4E:B0:0C:F7:F6:5A:FE:F6:07:D0:7B:72:E6:CA:FA`, valid April2021–April2031.

The existing matched-target inspector was run with `NODE_EXTRA_CA_CERTS=/tmp/tinesight-readonly-db/supabase-root.crt`, and its connection explicitly sets `sslmode=verify-full`. It performs only metadata SELECTs inside `BEGIN READ ONLY` and finishes with `ROLLBACK`. No global trust-store change, disabled certificate/hostname checks, production writes, credentials output or wrong-project query was used. The successful metadata result supersedes the earlier TLS-blocked statement in historical progress reports.
