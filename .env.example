# Copy this file to .env.local and fill in your values
# Get these from your Supabase project settings

# Supabase - Public (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Supabase - Server-side only (NEVER expose to client)
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
SUPABASE_JWT_SECRET="your-jwt-secret"

# Postgres - Direct connection
POSTGRES_URL="postgres://..."
POSTGRES_PRISMA_URL="postgres://..."
POSTGRES_URL_NON_POOLING="postgres://..."
POSTGRES_HOST="db.your-project.supabase.co"
POSTGRES_DATABASE="postgres"
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="your-password"

# =============================================================================
# Background Jobs & AI
# =============================================================================

# Trigger.dev - Background job processing
TRIGGER_SECRET_KEY="tr_dev_xxx"
TRIGGER_API_URL="https://api.trigger.dev"

# Google Gemini API - AI analysis and deer identification
GEMINI_API_KEY="your-gemini-api-key"

# =============================================================================
# SAM2 Pipeline (GPU Worker)
# =============================================================================

# SAM2 Feature Flag - Enable SAM2 pipeline (true/false)
SAM2_PIPELINE_ENABLED="false"

# SAM2 Worker URL - Base URL of Vast.ai GPU worker
SAM2_WORKER_URL="http://localhost:8000"
