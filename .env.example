# Copy this file to .env.local and fill in your values
# Get these from your Supabase project settings

# Supabase - Public (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Supabase - Server-side only (NEVER expose to client)
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
SUPABASE_JWT_SECRET="your-jwt-secret"

# Postgres - Direct connection
POSTGRES_URL="postgres://..."
POSTGRES_PRISMA_URL="postgres://..."
POSTGRES_URL_NON_POOLING="postgres://..."
POSTGRES_HOST="db.your-project.supabase.co"
POSTGRES_DATABASE="postgres"
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="your-password"

# =============================================================================
# Background Jobs & AI
# =============================================================================

# Trigger.dev - Background job processing
TRIGGER_SECRET_KEY="tr_dev_xxx"
TRIGGER_API_URL="https://api.trigger.dev"

# Google Gemini API - AI analysis and deer identification
GEMINI_API_KEY="your-gemini-api-key"

# Gemini model selection (optional, defaults to gemini-2.5-flash)
# gemini-3-flash-preview: Better accuracy, 67% more expensive ($0.50/1M in, $3.00/1M out)
# gemini-2.5-flash: Default, good balance ($0.30/1M in, $2.50/1M out)
GEMINI_MODEL="gemini-3-flash-preview"

# =============================================================================
# Maps
# =============================================================================

# Google Maps - Map display for location picker
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key"
